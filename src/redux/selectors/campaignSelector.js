export const getActiveCampaignId = (state) => {
  return state.campaign.activeCampaignId;
};

export const getCampaignOverviewDetails = (state) => {
  return state.campaign.campaignOverviewDetails;
};

